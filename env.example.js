// Optional runtime override file (reference only).
// If needed, inject this before env.js in index.html for environment-specific deploys.
window.__SOCIOMONKEY_ENV__ = {
  ENV: 'production',
  API_BASE: 'https://api.sociomonkey.ai/api',
}
