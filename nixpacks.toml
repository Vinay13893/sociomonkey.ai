[phases.setup]
nixPkgs = ["python311", "gcc", "libpq"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 2 -b 0.0.0.0:$PORT --timeout 120 --keep-alive 5 --log-level info wsgi:application"
